{
  "name": "Svelte Development",
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "USER_UID": "1000",
      "USER_GID": "1000"
    }
  },
  "runArgs": [
    "--security-opt",
    "apparmor=unconfined",
    "--security-opt",
    "seccomp=unconfined"
  ],
  "mounts": [
    "source=${env:SSH_AUTH_SOCK},target=/ssh-agent,type=bind",
    "source=${localEnv:HOME}/.gitconfig,target=/home/vscode/.gitconfig,type=bind"
  ],
  "remoteUser": "vscode",
  "containerEnv": {
    "SSH_AUTH_SOCK": "/ssh-agent"
  },
  "forwardPorts": [
    5173,
    4173,
    1455
  ],
  "portsAttributes": {
    "5173": {
      "label": "Vite dev server"
    },
    "4173": {
      "label": "Vite preview"
    },
    "1455": {
      "label": "Codex auth callback"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "yzhang.markdown-all-in-one",
        "davidanson.vscode-markdownlint",
        "redhat.vscode-yaml",
        "svelte.svelte-vscode",
        "anthropic.claude-code",
        "openai.codex"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "codex.model": "gpt-5-codex",
        "codex.reasoningEffort": "medium",
        "codex.defaultMode": "agent",
        "codex.enableAutoContext": true,
        "codex.allowFileEdits": true,
        "codex.allowTerminalCommands": "ask"
      }
    }
  },
  "features": {
    "ghcr.io/devcontainers/features/git:1": {},
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "postCreateCommand": "bash -lc \"mkdir -p ~/.claude && claude --version && mkdir -p ~/.codex && codex --version && npm install\"",
  "postAttachCommand": "npm run dev:host"
}
